# Local Config (Do Not Commit Secrets)

This folder contains machine-local configuration files.

- Local files must use `*.local.json` and are ignored by git.
- Example/template files can be versioned in root as `*.example`.

Current local file:
- `mcp_config.local.json` (moved from legacy `mcp_config.json`)
