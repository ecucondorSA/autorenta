/**
 * Helpers para operar con la API en tests (placeholders)
 * Implementar llamadas a endpoints de administración o scripts de seeding
 * según la infraestructura de test de cada entorno.
 */
export async function seedTestData() {
  // Placeholder: implementar creación de usuarios, vehículos y reservas
  // por ejemplo mediante una llamada a un endpoint admin o ejecutando SQL.
  return;
}

export async function cleanupTestData() {
  // Placeholder: limpiar datos creados por los tests
  return;
}
