{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "scripts": {
    "install": "npm install --loglevel error --strict-ssl false",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "your-dependencies": "^1.0.0"
  },
  "devDependencies": {
    "your-dev-dependencies": "^1.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
