
{
  "name": "autorenta",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nx run my-app:build",
    "affected": "nx affected",
    "print-affected": "nx print-affected",
    "format": "nx format:write",
    "format:write": "nx format:write",
    "format:check": "nx format:check",
    "update": "nx migrate latest",
    "show-migrations": "nx migrate show",
    "update-check": "nx migrate report",
    "version": "nx version",
    "help": "nx help"
  },
  "private": true,
  "devDependencies": {
    "@nx/devkit": "18.0.7",
    "nx": "18.0.7",
    "prettier": "~3.1.0",
    "typescript": "~5.3.3"
  }
}
