# Use this .npmrc file to configure your private registry
# Replace the following URL with the URL of your private registry
registry=https://your-private-registry.example.com

# Replace the following with your authentication token or credentials
# For example, using a token:
//your-private-registry.example.com/:_authToken=YOUR_AUTH_TOKEN

# Or, using username and password (less secure):
//your-private-registry.example.com/:username=YOUR_USERNAME
//your-private-registry.example.com/:_password=YOUR_ENCRYPTED_PASSWORD
//your-private-registry.example.com/:email=YOUR_EMAIL
