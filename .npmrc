# Add the following lines to authenticate with the private registry
# Replace <REGISTRY_URL> with the URL of your private registry
# Replace <AUTH_TOKEN> with your authentication token

# Example:
# @nx:registry=https://your-private-registry.com
# //your-private-registry.com/:_authToken=YOUR_ACTUAL_AUTH_TOKEN
