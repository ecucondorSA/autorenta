name = "autorent-doc-verifier"
main = "src/index.ts"
compatibility_date = "2024-10-23"

# AI binding para Cloudflare Workers AI
[ai]
binding = "AI"

# Variables de entorno
[vars]
ENVIRONMENT = "production"

# Secrets (configurar con wrangler secret put)
# VERIFICATION_TOKEN = "tu-token-secreto-aqui"
